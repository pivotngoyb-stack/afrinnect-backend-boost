// @ts-nocheck
/**
 * ExitIntentOffer — REMOVED
 * 
 * This component showed a fabricated "Offer expires in 10 minutes" countdown
 * and a fake discount percentage that was never validated server-side.
 * Renders nothing until a real promotional offer system is implemented
 * with server-validated discount codes.
 */

export default function ExitIntentOffer({ 
  isOnPricingPage = false,
  discountPercent = 20,
  onDismiss 
}) {
  return null;
}
